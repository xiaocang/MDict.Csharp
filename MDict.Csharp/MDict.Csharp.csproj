<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <ApplicationIcon>icon.ico</ApplicationIcon>
  </PropertyGroup>

  <PropertyGroup>
    <Version>0.1.3</Version>
    <PackageVersion>0.1.3</PackageVersion>
    <AssemblyVersion>0.1.3</AssemblyVersion>
    <InformationalVersion>0.1.3</InformationalVersion>
    <FileVersion>0.1.3</FileVersion>
    <PackageId>MDict.Csharp</PackageId>
    <PackageIcon>icon.png</PackageIcon>
    <Authors>Jack251970</Authors>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <CopyRight>Jack251970</CopyRight>
    <RepositoryUrl>https://github.com/xiaocang/MDict.Csharp</RepositoryUrl>
    <PackageProjectUrl>https://github.com/Jack251970/MDict.CSharp</PackageProjectUrl>
    <Description>*.mdx/*.mdd interpreter csharp implements, support mdict index file, with type-2 encryption support</Description>
    <PackageDescription>*.mdx/*.mdd interpreter csharp implements, support mdict index file, with type-2 encryption support</PackageDescription>
    <PackageTags>csharp; dictionary; mdx; mdd; glodendict; mdict</PackageTags>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BouncyCastle.Cryptography" Version="2.6.2" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="icon.ico" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\README.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <None Update="icon.png">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
